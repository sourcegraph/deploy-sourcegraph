apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    deploy: prometheus
  name: prometheus
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
  storageClassName: sourcegraph
