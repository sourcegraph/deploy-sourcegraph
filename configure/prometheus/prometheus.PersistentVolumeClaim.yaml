apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    deploy: prometheus
  name: prometheus
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: sourcegraph
  resources:
    requests:
      storage: 200Gi
