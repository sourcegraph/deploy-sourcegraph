apiVersion: v1
kind: Service
metadata:
  labels:
    app: private-docker-registry
  name: private-docker-registry
  namespace: default
spec:
  ports:
    - name: http
      port: 5000
      protocol: TCP
      targetPort: 5000
  selector:
    app: private-docker-registry
  type: ClusterIP
