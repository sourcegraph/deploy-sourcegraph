apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: private-docker-registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: sourcegraph
