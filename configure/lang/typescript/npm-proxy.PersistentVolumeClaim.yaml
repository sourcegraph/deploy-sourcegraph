apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: npm-proxy
    deploy: lang-typescript
  name: npm-proxy
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: sourcegraph
