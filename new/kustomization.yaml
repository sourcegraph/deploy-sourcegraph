apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- base/sourcegraph
components:
- components/StorageClass/gcp


# include monitoring

# ingress

# sizing

# custom set memory, cpu, etc.

# tls

# environment variable on gitserver: SRC_ENABLE_GC_AUTO=false

# remove certain fields (e.g., spec.clusterIP)

# todo: include constraint in cluster that nothing has root permissions
