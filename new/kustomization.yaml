apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - base/sourcegraph
  - base/monitoring
  - base/ingress
components:
  - components/StorageClass/gcp

# TODO
# - [x] include monitoring
# - [x] ingress
# - tls
# - sizing
# - custom set memory, cpu, etc.
# - environment variable on gitserver: SRC_ENABLE_GC_AUTO=false
# - remove certain fields (e.g., spec.clusterIP)
# - Is there a way toinclude constraint in cluster that nothing has root permissions?
