apiVersion: v1
kind: ConfigMap
metadata:
  name: sourcegraph-frontend-env
# MAKE YOUR CHANGES AFTER LINE 6
data:
  # PGDATABASE: $NEW_VALUE
  # PGHOST: $NEW_VALUE
  # PGPORT: $NEW_VALUE
  # PGSSLMODE: $NEW_VALUE
  # PGUSER: $NEW_VALUE
  # CODEINSIGHTS_PGDATASOURCE: $NEW_VALUE
  # CODEINTEL_PGDATABASE: $NEW_VALUE
  # CODEINTEL_PGHOST: $NEW_VALUE
  # CODEINTEL_PGPORT: $NEW_VALUE
  # CODEINTEL_PGSSLMODE: $NEW_VALUE
  # CODEINTEL_PGUSER: $NEW_VALUE