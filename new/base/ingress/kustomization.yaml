apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ingress-nginx
metadata:
  labels:
    deploy: sourcegraph
resources:
  # Please use the "otel-collector" component to deploy otel-collector
  - cloud-generic.yaml
  - mandatory.yaml
patchesJson6902:
  - target:
      name: .*
    patch: |-
      - op: replace
        path: /metadata/labels/deploy
        value: sourcegraph
