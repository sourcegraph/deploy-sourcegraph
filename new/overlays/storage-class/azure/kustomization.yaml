apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ns-sourcegraph
resources:
  # Replace the version number to upgrade / downgrade
  - ../../../base/monitoring
  - ../../../base/sourcegraph
components:
  - ../../components/StorageClass/azure          # Create a StorageClass resource for aws