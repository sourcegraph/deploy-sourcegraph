apiVersion: apps/v1
kind: Deployment
metadata:
  name: sourcegraph-frontend
spec:
  template:
    spec:
      containers:
      - name: frontend
        env:
        - name: PGDATABASE
          value: $NEW_PGDATABASE
        - name: PGHOST
          value: $NEW_PGHOST
        - name: PGPORT
          value: $NEW_PGPORT
        - name: PGSSLMODE
          value: disable
        - name: PGUSER
          value: $NEW_PGUSER