apiVersion: v1
kind: ConfigMap
metadata:
  name: sourcegraph-frontend-env
data:
  PGDATABASE: $STAGING_PGDATABASE
  PGHOST: $STAGING_PGHOST
  PGPORT: $STAGING_PGPORT
  PGSSLMODE: disable
  PGUSER: $STAGING_PGUSER