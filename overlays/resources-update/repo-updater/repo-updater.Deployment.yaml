apiVersion: apps/v1
kind: Deployment
metadata:
  name: repo-updater
spec:
  template:
    spec:
      containers:
      - name: repo-updater
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: "1"
            memory: 500Mi