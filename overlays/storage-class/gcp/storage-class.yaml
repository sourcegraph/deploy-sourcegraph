kind: StorageClass
apiVersion: storage.k8s.io/v1beta1
metadata:
  name: default
provisioner: kubernetes.io/gce-pd
parameters:
  type: pd-ssd
  # TODO(you): configure the zone your Sourcegraph instance is deployed to.
  # zone: us-west1-a