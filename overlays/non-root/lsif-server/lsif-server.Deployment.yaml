apiVersion: apps/v1
kind: Deployment
metadata:
  name: lsif-server
spec:
  template:
    spec:
      containers:
      - name: lsif-server
        securityContext:
          # Required to prevent escalations to root.
          allowPrivilegeEscalation: false
          runAsUser: 100
          runAsGroup: 101
