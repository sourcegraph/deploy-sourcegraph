apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    category: rbac
    deploy: sourcegraph
    rbac-admin: escalated
  name: sourcegraph-frontend
rules:
  - apiGroups:
      - ''
    resources:
      - endpoints
      - services
    verbs:
      - get
      - list
      - watch
