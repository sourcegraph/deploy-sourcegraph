apiVersion: v1
kind: Service
metadata:
  labels:
    app: lsif-server
    deploy: sourcegraph
  name: lsif-server
spec:
  ports:
  - name: http
    port: 3186
    targetPort: http
  selector:
    app: lsif-server
  type: ClusterIP
