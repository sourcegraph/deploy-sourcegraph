apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/port: "9090"
    sourcegraph.prometheus/federate: "true"
  labels:
    app: pci-worker
    deploy: sourcegraph
  name: pci-worker
spec:
  ports:
  - name: server
    port: 3188
    targetPort: server
  - name: prometheus
    port: 9090
    targetPort: prometheus
  selector:
    app: pci-worker
  type: ClusterIP
