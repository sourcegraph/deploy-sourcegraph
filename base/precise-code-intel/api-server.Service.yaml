apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/port: "9090"
    sourcegraph.prometheus/federate: "true"
  labels:
    app: pci-api-server
    deploy: sourcegraph
  name: pci-api-server
spec:
  ports:
  - name: server
    port: 3186
    targetPort: server
  - name: prometheus
    port: 9090
    targetPort: prometheus
  selector:
    app: pci-api-server
  type: ClusterIP
