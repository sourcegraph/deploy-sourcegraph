apiVersion: v1
kind: Service
metadata:
  annotations:
    description: Dummy service that prevents gitserver-n pods from being scheduled on the same node if possible.
  labels:
    type: gitserver
  name: gitserver
spec:
  clusterIP: None
  ports:
  - name: unused
    port: 10811
    targetPort: 10811
  selector:
    type: gitserver
  type: ClusterIP
  
