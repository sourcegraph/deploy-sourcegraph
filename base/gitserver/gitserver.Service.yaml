apiVersion: v1
kind: Service
metadata:
  labels:
    deploy: sourcegraph
    type: gitserver
  name: gitserver
spec:
  clusterIP: None
  ports:
    - name: unused
      port: 10811
      targetPort: 10811
  selector:
    type: gitserver
  type: ClusterIP
