apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    deploy: sourcegraph
    disk: codeintel-db
  name: codeintel-db
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 3Ti
  claimRef:
    namespace: dogfood-k8s
    name: codeintel-db
  storageClassName: sourcegraph
