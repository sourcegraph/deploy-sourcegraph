apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    deploy: sourcegraph
    disk: codeintel-db
  name: codeintel-db
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 3Ti
  claimRef:
    namespace: dogfood-k8s
    name: codeintel-db
  gcePersistentDisk:
    fsType: ext4
    pdName: efritz-codeintel-db-real
  storageClassName: sourcegraph
