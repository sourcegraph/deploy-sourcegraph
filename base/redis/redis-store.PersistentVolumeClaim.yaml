apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    deploy: sourcegraph
  name: redis-store
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: sourcegraph
  resources:
    requests:
      storage: 100Gi
