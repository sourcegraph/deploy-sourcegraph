apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    deploy: sourcegraph
  name: redis-cache
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: sourcegraph
  resources:
    requests:
      storage: 100Gi
