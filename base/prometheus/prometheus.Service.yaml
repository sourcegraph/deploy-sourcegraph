apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus
    deploy: sourcegraph
    rbac-admin: non-escalated
  name: prometheus
spec:
  ports:
  - name: http
    port: 30090
    targetPort: http
  selector:
    app: prometheus
  type: ClusterIP
