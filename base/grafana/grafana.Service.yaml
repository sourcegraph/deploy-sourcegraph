apiVersion: v1
kind: Service
metadata:
  labels:
    app: grafana
    deploy: sourcegraph
    sourcegraph-rbac-admin: non-escalated
  name: grafana
spec:
  ports:
  - name: http
    port: 30070
    targetPort: http
  selector:
    app: grafana
  type: ClusterIP
