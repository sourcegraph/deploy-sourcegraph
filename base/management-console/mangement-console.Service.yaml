apiVersion: v1
kind: Service
metadata:
  labels:
    app: management-console
    deploy: sourcegraph
  name: management-console
spec:
  ports:
  - name: http
    port: 6075
    targetPort: http
  selector:
    app: management-console
  type: ClusterIP
