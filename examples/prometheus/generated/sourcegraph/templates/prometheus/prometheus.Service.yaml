##---
# Source: sourcegraph/templates/prometheus/prometheus.Service.yaml
apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus
    deploy: sourcegraph
  name: prometheus
spec:
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app: prometheus
  type: ClusterIP
