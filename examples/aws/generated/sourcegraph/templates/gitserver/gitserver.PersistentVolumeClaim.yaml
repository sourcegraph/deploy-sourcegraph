##---
# Source: sourcegraph/templates/gitserver/gitserver.PersistentVolumeClaim.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    volume.beta.kubernetes.io/storage-class: default
  deploy: sourcegraph
  name: gitserver-1
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi

