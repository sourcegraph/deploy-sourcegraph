apiVersion: v1
kind: Service
metadata:
  labels:
    deploy: sourcegraph
    app: ss-test
  name: ss-test
spec:
  clusterIP: None
  ports:
  - name: unused
    port: 10812
    targetPort: 10812
  selector:
    app: ss-test
  type: ClusterIP
