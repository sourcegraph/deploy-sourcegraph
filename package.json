{
  "scripts": {
    "prettier": "prettier  --write --list-different --config prettier.config.js",
    "prettier-all": "prettier '**/{*.{js?(on),ts?(x),graphql,md,scss,yaml},.*.js?(on)}'"
  },
  "devDependencies": {
    "@sourcegraph/prettierrc": "3.0.1",
    "prettier": "^2.0.4"
  }
}
