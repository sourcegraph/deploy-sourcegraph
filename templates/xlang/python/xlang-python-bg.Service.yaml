{{- $args := (dict "langservers" .Values.conf.langservers "lang" "python") -}}
{{- include "hasLanguage" $args -}}
{{- if $args.ret -}}
apiVersion: v1
kind: Service
metadata:
  labels:
    app: xlang-python-bg
  name: xlang-python-bg
spec:
  ports:
  - name: lsp
    port: 2087
    targetPort: lsp
  selector:
    app: xlang-python-bg
  type: ClusterIP
{{ end -}}
