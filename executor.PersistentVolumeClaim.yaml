kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: executor-scratch
  labels:
    deploy: sourcegraph
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
